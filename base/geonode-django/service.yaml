apiVersion: v1
kind: Service
metadata:
  name: django
  labels:
    app: django
spec:  
  selector:
    app: geonode
    tier: frontend
  type: ClusterIP
  ports:
  - name: uwsgi
    port: 8000
    protocol: TCP
    targetPort: 8080
