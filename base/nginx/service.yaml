apiVersion: v1
kind: Service
metadata:
  name: geonode
  labels:
    app: geonode
spec:
  selector:
    app: geonode
    tier: frontend
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  - name: https
    port: 443
    protocol: TCP
    targetPort: 443
